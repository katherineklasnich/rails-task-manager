<h1>My Tasks</h1>
  <ul>
    <% @tasks.each do |task| %>
    <input type="checkbox" name="box" value="task"> <%= link_to task.title, task_path(task) %><br>
    <% end %>
  </ul>
  <h4><%= link_to "+ Add a new task", tasks_new_path %></h4>
